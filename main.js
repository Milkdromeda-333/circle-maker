const r = Number(prompt('Enter a number to make a circle'));

const circle = {
	getCircumference(num) {
		num = r;
		circumference = num * (3.14 * 2)
	console.log(circumference)
	}
}

circle.getCircumference();
